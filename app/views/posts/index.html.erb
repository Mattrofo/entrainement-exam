<h1> All posts </h1>


<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <p><%= post.description %></p>
  <p>
    <a href="<%= post_path(post.id) %>" class="btn btn-primary"> Read the post </a>
  </p>
<% end %>
